# Example Vite environment variables for the OctoFit Tracker frontend.
# Copy this file to .env.local and set the Codespace name when running in GitHub Codespaces.
# When VITE_CODESPACE_NAME is undefined, the app uses localhost:8000/api as a safe fallback.

VITE_CODESPACE_NAME=your-codespace-name
